extends layout

block content

	{{members.count}}

	div(ng-controller="SonnetIndexCtrl")

		div(ng-repeat="sonnet in sonnets | orderBy:'number'")

			h3 
				a(href="/sonnets/{{sonnet.number}}") {{sonnet.title}}
